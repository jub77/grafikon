<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>net.parostroj</groupId>
    <artifactId>grafikon</artifactId>
    <version>1</version>
    <packaging>pom</packaging>
    <name>grafikon</name>

    <properties>
        <maven.build.timestamp.format>yyyyMMdd</maven.build.timestamp.format>
    </properties>

    <profiles>
        <profile>
            <id>development</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <grafikon.version>dev</grafikon.version>
                <grafikon.version.build>${grafikon.version}-${maven.build.timestamp}</grafikon.version.build>
                <changeSet>build</changeSet>
            </properties>
        </profile>
        <profile>
            <id>snapshot</id>
            <activation>
                <property>
                    <name>snapshot</name>
                </property>
            </activation>
            <properties>
                <grafikon.version>0.10-SNAPSHOT</grafikon.version>
                <grafikon.version.build>${grafikon.version}-${changeSet}</grafikon.version.build>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>buildnumber-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <phase>validate</phase>
                                <goals>
                                    <goal>hgchangeset</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>release</id>
            <activation>
                <property>
                    <name>release</name>
                </property>
            </activation>
            <properties>
                <grafikon.version>0.10</grafikon.version>
                <grafikon.version.build>${grafikon.version}</grafikon.version.build>
                <changeSet>release</changeSet>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <configuration>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                        <manifestEntries>
                            <Implementation-Build>${grafikon.version}-${changeSet}</Implementation-Build>
                            <Built-Date>${maven.build.timestamp}</Built-Date>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <modules>
        <module>grafikon-model</module>
        <module>grafikon-save</module>
        <module>grafikon-ls</module>
        <module>grafikon-ls4</module>
        <module>grafikon-templates</module>
        <module>grafikon-output</module>
        <module>grafikon-gui-components</module>
        <module>grafikon-net</module>
        <module>grafikon-gui</module>
    </modules>
</project>
