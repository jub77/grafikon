<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>net.parostroj</groupId>
    <artifactId>grafikon</artifactId>
    <version>0.13</version>
    <packaging>pom</packaging>
    <name>grafikon</name>

    <properties>
        <maven.build.timestamp.format>yyyyMMdd</maven.build.timestamp.format>
        <timestamp>${maven.build.timestamp}</timestamp>
    </properties>

    <profiles>
        <profile>
            <id>development</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <grafikon.version>dev</grafikon.version>
                <grafikon.version.build>${grafikon.version}-${maven.build.timestamp}</grafikon.version.build>
                <grafikon.version.show>${grafikon.version.build}</grafikon.version.show>
                <changeSet>build</changeSet>
            </properties>
        </profile>
        <profile>
            <id>versioned</id>
            <activation>
                <property>
                    <name>versioned</name>
                </property>
            </activation>
            <properties>
                <grafikon.changeset>current</grafikon.changeset>
                <grafikon.version>${project.version}</grafikon.version>
                <grafikon.version.build>${grafikon.version}-${maven.build.timestamp}-${grafikon.changeset}</grafikon.version.build>
                <grafikon.version.show>${grafikon.version}-${maven.build.timestamp}</grafikon.version.show>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.0</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                        <manifestEntries>
                            <Implementation-Build>${grafikon.version.build}</Implementation-Build>
                            <Built-Date>${maven.build.timestamp}</Built-Date>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>JavaNet</id>
            <url>http://download.java.net/maven/2</url>
        </repository>
    </repositories>

    <modules>
        <module>grafikon-model</module>
        <module>grafikon-save</module>
        <module>grafikon-ls</module>
        <module>grafikon-ls4</module>
        <module>grafikon-templates</module>
        <module>grafikon-output2</module>
        <module>grafikon-gui-components</module>
        <module>grafikon-net</module>
        <module>grafikon-gui</module>
        <module>grafikon-start</module>
        <module>grafikon-webstart</module>
        <module>grafikon-assembly</module>
    </modules>
</project>
