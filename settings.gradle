pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }/*
    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == "net.parostroj.versioning") {
                useModule "net.parostroj.versioning:version-plugin:1.0.0"
            }
        }
    }*/
}

enableFeaturePreview('IMPROVED_POM_SUPPORT')

//includeBuild 'grafikon-version-plugin'

rootProject.name = 'grafikon'

include 'grafikon-model'
include 'grafikon-save'
include 'grafikon-ls'
include 'grafikon-ls4'
include 'grafikon-templates'
include 'grafikon-output2'
include 'grafikon-gui-components'
include 'grafikon-gui'
include 'grafikon-start'
include 'grafikon-assembly'
include 'grafikon-webstart'
